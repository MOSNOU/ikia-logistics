"use client";

// Side-effect-only CSS import for Leaflet. Imported by every map component
// so the tile layer + marker styles render correctly.
import "leaflet/dist/leaflet.css";

export function LeafletStyles() {
  return null;
}
