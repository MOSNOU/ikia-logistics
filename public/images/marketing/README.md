# Marketing image assets (placeholders)

These raster assets are referenced by the marketing pages but are **not yet
provided**. Until real artwork is added, components render branded gradient
placeholders (no broken images). Drop the real `.webp` files here and the
`persona.image` paths in `content/personas/*` will line up.

Expected files (TODO — provide approved artwork, do not use remote stock):

- `hero.webp`
- `forwarders.webp`
- `shippers.webp`
- `enterprise.webp`
- `carriers.webp`

When adding, render with `next/image` (Persian `alt` text, responsive sizing).
